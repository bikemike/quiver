if MAEMO
desktopdir = $(datadir)/applications/hildon
else
desktopdir = $(datadir)/applications
endif

desktop_DATA = quiver.desktop

gladedir = $(datadir)/quiver/glade
glade_DATA = quiver.glade

EXTRA_DIST = $(desktop_DATA) $(glade_DATA)
